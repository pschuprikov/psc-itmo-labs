\section{Построение береговых координат}\label{coordinate_system}
В данной главе будет определено некоторое непрерывное отображение, сопоставляющее точке $(x,y)$ 
в мировой системе координат (игнорируя высоту) пару вещественных значений 
$(\xi_x, \xi_y)$, которые мы далее будем называть береговыми координатами:
\begin{equation}\label{base_tc}
 (\xi_x, \xi_y) = f(x, y)
\end{equation}

При этом изолинии $\xi_y = const$ должны быть визуально 
похожи на множество точек, расположенных на одном расстоянии от береговой линии и для 
определенность пусть значение $\xi_y$ будет убывать при приближении к берегу со стороны моря. 
Таким образом, если параметром эффекта является некоторая функция береговых координат 
$g(\xi_x. \xi_y)$, то для получения того же самого эффекта, движущегося к берегу со 
скоростью $v$, достаточно следующим образом изменить параметризацию: 
\begin{equation}
g'(\xi_x. \xi_y) = g(\xi_x, \xi_y + v t),
\end{equation}
где $t$ --- время.

\subsection{Кусочно-линейный скелет}
В данном методе используется метод представления полигона (береговая линия также является полигоном),
известный как \emph{кусочно-линейный скелет} (англ.~Straight Skeleton). 
С помощью кусочно-линейного скелета можно быстро получить заданное \emph{смещение} заданного полигона.
Более формальное определение операции смещения будет дано далее.

Определим кусочно-линейный скелет так, как это сделано в \cite{skeleton_theory}.
Предположим, что дан некоторый полигон. Тогда вообразим здание, 
представляющее собой прямоугольную призму с основанием, границей которого является имеющийся полигон. 
Далее рассмотрим структуру крыши этого здания, если наклон крыши всюду один и тот же 
(см.~рис.~\ref{fig_skeleton_roof}).
Оказывается, что структура получившейся крыши не зависит от величины наклона, а
полученное множество ребер и вершин крыши, спроецированное на плоскость основания,
является кусочно-линейным скелетом исходного полигона.

\drawfigurex{H}{width=10cm}{pics/skeleton.eps}{Кусочно-линейный скелет как <<крыша>>
с фиксированным углом наклона над полигональным основанием}{fig_skeleton_roof}

\emph{Смещение} полигона на величину $\mathrm{offset} \le 0$, определим следующим образом:
пусть наклон крыши составляет $\alpha$ радиан, тогда рассмотрим сечение крыши, параллельное 
основанию и расположенное на высоте $$h = (-\mathrm{offset}) \cdot \tan{\alpha},$$ относительно него. 
Проекция данного сечения на плоскость основания и является смещением. Заметим, что 
смещение полигона, в общем случае, может представлять собой множество полигонов или вовсе быть пустым.
Если продолжить крышу <<вниз>> относительно основания, то аналогичным образом определяется
смещение полигона на величину $\mathrm{offset} > 0$. Смещения полигона изображены серыми 
линиями на рис.~\ref{fig_skeleton_roof}.

\subsection{Береговые координаты на основе кусочно-линейной функции}
В данном подразделе вводиться система координат, основанная на смещениях полигона. При этом считается
, что рассматриваемые смещения полигона береговой линии остаются связными без пересечений. 
В процессе рассматривается несколько возможных способов задать координаты $(\xi_x, \xi_y)$,
итеративно улучшая предыдущий результат.
Итак, через некоторую точку $(x,y)$ (см.~рис.~\ref{fig_skeleton_base}) проходит не более 
чем одно связное смещение полигона береговой линии. Если данное смещение $l(y_0)$ на величину $y_0$
 существует, то определим береговые координат $(\xi_x, \xi_y)$ следующим образом:
\begin{equation}\label{tc_first}
    (\xi_x(x,y), \xi_y(x,y)) = (x_{y_0}, y_0),
\end{equation}
где $x_{y_0}$ --- длина подотрезка $l(y_0)$ от некоторого положения $\xi_x = 0$ до точки $(x,y)$ 
(см.~рис.~\ref{fig_skeleton_roof}).

\drawfigure{pics/skeleton_base.eps}{Береговая система координат без сглаживания. Красным и синим
выделены береговая линия ($y = 0$) и множество, на котором $x = 0$, соответственно; 
пунктиром обозначены ребра кусочно-линейного скелета; $l(y_o)$ и $l(y_1)$ --- смещения полигона береговой 
линии на величины $y_0$ и $y_1$ соответственно.}{fig_skeleton_base}

\subsection{Интерполяция координат}
Для работы методов, приведенных в следующем разделе и отвечающих за нанесение разрабатываемых 
эффектов на поверхность воды, необходима возможность быстро восстанавливать координаты
$(\xi_x,\xi_y)$, зная $(x,y)$. Пусть здесь и далее $y_{max}$ --- максимальная
величина смещения полигона, а $y_{min}$ --- минимальная (допускается $y_{min} < 0$), 
и пусть имеется некоторое представление кусочно-линейного скелета, <<покрывающее>> эти границы смещений.

\subsubsection{Разбиение кусочно-линейного скелета}\label{subdivision}
Грани кусочно линейного скелета разбиваются на четырехугольники и треугольники, внутри которых формула
выражающая зависимость $\vec{\xi} = f(\bvec{x})$ имеет простой вид. Каждая грань 
$\bvec{v}_0\bvec{v}_1 \ldots \bvec{v}_n$ 
,где $\bvec{v}_0\bvec{v}_1$ --- ребро исходного полигона, кусочно-линейного скелета подразбивается 
на трапеции следующим образом:
\begin{enumerate}
\item Строится множество $Y= \{y_i\} = \{y_{min}, y_{max}\} \cup \left(\bigcup\limits_{i=0}^n \{distance(\bvec{v}_0, l)\}\right)$, где $l$ -- прямая, 
проходящая через $\bvec{v}_0$ и $\bvec{v}_1$.
\item Исходная грань $\bvec{v}_0\bvec{v}_1 \ldots \bvec{v}_n$ разбивается прямыми $l_i$, параллельными $l$, 
находящимися на расстоянии $y_i$ от $l$. Элементом разбиения являются либо трапеции, основания которых
параллельны $l$, либо треугольник, одна из сторон которого также параллельна $l$.
\end{enumerate}

\subsubsection{Оценка числа элементов разбиения}
Заметим, что согласно \cite{skeleton_theory} кусочно-линейный скелет, построенный для 
внутренней части полигона с $n$ вершинами, содержит $2 n - 2$ вершины (включая вершины полигона).
Аналогичная оценка верна и для внешней части, если считать, что каждому бесконечному ребру скелета 
соответствует своя вымышленная вершина на бесконечности. Таким образом общее число вершин можно ограничить
как $3 n$. Заметим, что через каждую вершину в процессе разбиения будет проведено не более двух прямых, а
значит число трапеций можно ограничить как $6n = O(n)$.

Пусть имеется точка $(x,y)$ и требуется найти соответствующие ей координаты 
$(\xi_x, \xi_y)$, если точка лежит внутри трапеции, иначе нужно сказать, что точка
не лежит в данной трапеции. Случай треугольника является частным случаем трапеции, поэтому он
рассмотрен не будет. Дальнейшие формулы будут использовать грань $bckl$ кусочно линейного скелета 
с рис.~\ref{fig_skeleton_base}, а также рис.~\ref{fig_skeleton_interpolation}. 
Обозначим за $(\xi_x^p, \xi_y^p)$ береговые координаты в точке $\bvec{p}$.

\drawfigure{pics/skeleton_interpolation.eps}{Подразбиение грани $bckl$ на трапецию и треугольник для
быстрого определения береговых координат $(\xi_x,\xi_y)$, соответствующих точке $(x,y)$.}
{fig_skeleton_interpolation}

\subsubsection{Вычисление координат внутри трапеции}\label{coord_calculation}
Рассмотрим теперь процесс вычисления координат $\bvec{x} = (\xi_x,\xi_y)$ внутри трапеции
$bmkc$:
\begin{enumerate}
\item Вычислим нормализованную вертикальную координату внутри трапеции:
\begin{equation}
u_n = (\bvec{x} - \bvec{m}) \cdot \bvec{u} / (\xi_y^m - \xi_y^b).
\end{equation}
Если $u_n \notin [0,1]$, то точка не лежит внутри трапеции.
\item Определим отрезок, параллельный $\bvec{b}\bvec{c}$, проходящий через $\bvec{x}$:
\begin{equation}
\begin{array}{c}
\bvec{p} =  (1 - u_n) \bvec{m} + u_n \bvec{b} \\
\bvec{p'} = (1 - u_n) \bvec{k} + u_n \bvec{c}.
\end{array}
\end{equation}
\item Вычислим нормализованную горизонтальную координату внутри трапеции:
\begin{equation}
s_n = \frac{(\bvec{x} - \bvec{p}) \cdot \bvec{s}}{||\bvec{p} - \bvec{p}||}.
\end{equation}

Если $s_n \notin [0,1]$ то точка не лежит внутри трапеции.
\item Линейно интерполируем $\xi_x$ на отрезок $\bvec{p}\bvec{p}'$:
\begin{equation}
\begin{array}{c}
\xi_x^p = (1 - u_n) \xi_x^m + u_n \xi_x^b \\
\xi_x^{p'} = (1 - u_n) \xi_x^k + u_n \xi_x^k.
\end{array}
\end{equation}
\item $(\xi_x, \xi_y) = ((1 - s_n) \xi_x^p + s_n\xi_x^{p'}, (1 - u_n) \xi_y^m + u_n \xi_y^b)$.
\end{enumerate}

\subsection{Интерполяция сплайнами}
Метод вычисления береговых координат, предложенный в предыдущем разделе, обладает следующим существенным
недостатком: Множество точек $(x,y)$, для которых $\xi_y(x,y) = const$,
представляет собой ломаную. Из-за этого, у для любого эффекта, существенно опирающегося на 
$\xi_y$, будут характерны резкие изменения в ориентации эффекта, происходящие в вершинах ломаной.
Подобные артефакты, в данном случае, существенно снижают визуальную привлекательность эффекта и его 
реалистичность, потому как прибрежные волны изломов не испытывают и меняют направление своего движения
плавно.

\subsubsection{Сплайн Катмула-Рома}
В данной работе решено применить метод интерполяции сплайнами.
Существует множество различных вариаций данного метода, применяемых для решения различных
задач компьютерной графики (см.~\cite{math3d}). В данной работе решено было остановится на 
кубическом сплайне Катмула-Рома (англ.~Catmull-Rom), используемом обычно для интерполяции между 
ключевым кадрами для создания плавной анимации. Этот вид сплайна был выбран из-за простоты его вычисления,
а также из-за того, что каждая опорная точка принадлежит сплайну. 

Рассмотрим сплайн в общих чертах: пусть имеется некоторая последовательность опорных точек и 
${\bvec{p_0}, \bvec{p_1}, \bvec{p_2}, \bvec{p_3}}$ --- некоторая непрерывная подпоследовательность,
тогда для вычисление сплайна на отрезке между $\bvec{p_1}\bvec{p_2}$ используется следующее
приближение производной сплайна в точках $\bvec{p_1}$ и $\bvec{p_2}$:
\begin{equation}
\begin{array}{c}
    \bvec{p'_1} = \frac{\bvec{p_2} - \bvec{p_0}}{||\bvec{p_2} - \bvec{p_1}|| + ||\bvec{p_1} - \bvec{p_0}||} \\
    \bvec{p'_2} = \frac{\bvec{p_3} - \bvec{p_1}}{||\bvec{p_3} - \bvec{p_2}|| + ||\bvec{p_2} - \bvec{p_1}||}
\end{array}
\end{equation}

Затем вводится параметр $t \in [0,1]$, и сплайн на рассматриваемом отрезке параметрически задается как:
\begin{equation}\label{spline}
    \bvec{p}(t) = (2 t^3 - 3 t^2 + 1)\bvec{p_1} + (t^3 - 2 t^2 + t) \bvec{p'_1} + 
    (-2 t^3 + 3 t^2)\bvec{p_2} + (t^3 - t^2) \bvec{p'_2}.
\end{equation}

\subsubsection{Сглаживание смещений береговой линии}
Базовая идея следующая: каждое смещение полигона сгладить с помощью рассмотренного ранее
сплайна, и при вычислении $\xi_y$ для точки $\bvec{x}$ использовать величину такого смещения, 
интерполяция сплайном которой проходит через $\bvec{x}$. 

Однако, данный метод имеет ряд недостатков: во-первых, для этого необходимо было решить 
два кубических уравнения для каждой из координат; во-вторых, смещение, сплайн которой проходит через
точку, само может не проходить через рассматриваемую трапецию или треугольник. Поскольку данные 
вычисления предполагалось проводить для каждого фрагмента, который покрывает эффект (подробнее далее), 
то необходимо было упростить вычисления как алгоритмически, так и в смысле требуемого для выполнения 
числа инструкций. Таким образом, был разработан альтернативный метод.

Пусть в каждой вершине кусочно-линейного скелета вычислена аппроксимация производной согласно
уравнению \ref{spline}. Обозначим производную в вершине $\bvec{x}$ как $\dot{\bvec{x}}$. Тогда ко второму
пункту вычисления координат (см.~\ref{coord_calculation}) добавляется аналогичная линейная
интерполяция производных $\bvec{\dot{p}}$ и $\bvec{\dot{p}'}$. Полученной информации достаточно для
того, чтобы построить сплайн через точки $\bvec{p}$ и $\bvec{p'}$. Пусть далее, $\bvec{x'}$ --- точка
сплайна, соответствующая параметру $t = s_n$, тогда новое значение $\tilde{\xi}_y$ 
вычисляется как (см.~рис.~\ref{fig_interpolation_sample}):
\begin{equation}
    \tilde{\xi}_y = \xi_y + (\bvec{x'} - \bvec{x}) \cdot \bvec{u}.
\end{equation}

\drawfigure{pics/interpolation_sample.eps}{Модификация береговой координаты $\xi_y$ на основе
сплайна \emph{Catmull-Rom}.}
{fig_interpolation_sample}

\subsection{Отражение координат}\label{reflection}
Последняя модификация призвана решить две связанные проблемы: 
\begin{itemize}
\item Необходимо, чтобы координата $\xi_x$
с одной и другой стороны от начала отсчета ($\xi_x = 0$) совпадали.
\item На рис.~\ref{fig_skeleton_base} длина смещений $l(y_0)$ и $l(y_1)$ различна, а также в точках 
ненулевой кривизны береговой линии, фактическая скорость вдоль $l(y_0)$ и $l(y_1)$ различна, при 
фиксированной угловой.
\end{itemize}

Первый недостаток приводит к разрыву координат вблизи $\xi_x = 0$, а второй к тому, что градиент
береговых координат с ростом $\xi_x$ смещается вдоль берега, из-за чего предложенный 
в начале раздела метод анимации эффекта, действует не <<к берегу>>, а под значительным к нему 
(берегу) углом.

Обе обозначенные проблемы решены следующим образом: отрезки полигона береговой линии разбиваются на 
последовательности, суммарная длина отрезков внутри каждой из которых не превышает определенной 
настраиваемой величины $\mathrm{maxlen}$. Рассмотрим множество ребер кусочно-линейного скелета, 
исходящих из крайних точек какой-либо из получившихся подпоследовательностей: в точке пересечения
любого смещения $l$ с одним из таких ребер, значение $\xi_x$ вдоль данного смещения 
становиться <<сбрасывается>> в ноль. Рассмотрим теперь подмножество $\delta l$ произвольного смещения 
$l(y)$: чтобы в начале и конце $\delta l$ значение $\xi_x$ обращалось в ноль,
и при этом сохранялась непрерывность, $\xi_x$ <<отражается>> в середине $\delta l$.

Более формально: пусть длина $\delta l$ равна $s$, и пусть некоторая точка $\bvec{x}$ лежит на $\delta l$, причем
расстояние вдоль $l$ от начала $\delta l$ до равно $d$, тогда:
\begin{equation}
\xi_x(\bvec{x}) = \left| \frac{s}{2} - \left|d - \frac{s}{2}\right| \right|
\end{equation}